// Font loading via next/font removed due to build-time network restrictions.
// Fonts are now imported manually in head.js and applied via globals.css.

const inter = { className: '' };

export default inter;
